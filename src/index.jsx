import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App.jsx"; // make sure App.jsx exists in the same folder

ReactDOM.createRoot(document.getElementById("root")).render(<App />);
